PROJECTO CRUD

Requisitos:
	El HTML solo contienen DIVs
		DIVs están vacios y solo tienen una ID
	El resto se tienen que generar con JS
	
Objetivo 1:
	Al abrir la app, solo se verá una tabla. Si no hay nada, mustra un texto
	El boton para añadir entradas elimina toda la tabla y genera el formulario
		Al eliminar el primer DIV, se muestra el segundo DIV (formulario)
			Toda la información se guarda en un array JSON
			Todos debe tener un campo autonumerico (ID)
			Debe tener tener campos obligatios (min 3) y otras opcionales
			REGEX (min 2)
			EVENTS (min 3, 2 de raton y 1 de teclado)
			Al guardar, se borra el formulario y se genera la tabla
	Doble click
		Elimina la entrada
			Se elimina la tabla y entrada JSON
			NO se puede borrar la tabla y volver a crearla	
	NO boton refresh

Objetivo 2:
	DIV de modificar entrada
	Borrar tabla y generar el formulario

Objetivo 3:
	Dentro de un regisre JSON, debe tener otro Array JSON
	Nuevo DIV
		Muestra el registro principal
		Boton para crear mas sub-registros
		Boton para mostrar la tabla principal (?)
		Doble Click borrar sub-registros
		